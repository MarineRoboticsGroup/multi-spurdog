from setuptools import setup

setup(
    name='spurdog_acomms',
    version='0.1.0',
    packages=['spurdog_acomms_utils'],
    install_requires=[
        # Add any dependencies here, if needed
    ],
)
